## 版本迭代记录

> 按 AGENTS.md §四版本迭代记录规范维护。版本号规则：SemVer（主版本.次版本.修订号）。

| 版本号 | 日期 | 摘要 |
| --- | --- | --- |
| v0.0.1 | 2026-07-27 | 设计文档体系落盘：00-设计总览、01-愿景与定位、02-架构设计、03-技术栈、04-模块全景、05-路线图、06-需求文档、07-立项准备、11 份接口契约（A-提供方接口 至 K-主题系统）、3 份关键 ADR + 决策索引。无代码产出。设计层主线收口完成。 |
| v0.0.2 | 2026-07-29 | 设计文档 W0 收敛：依据 W0 设计文档审计完成 P0/P1/P2 全部修订——M10 拆分 lite/完整、覆盖率阶梯单一真源、版本时间点统一（签名 v0.4/撤销 v0.3/session restore v0.5）、契约 B/F 技术硬伤修复、新增 M23 Download Manager、新决策 CP6/CP7/TP6/PC8/OM10；决策口径统一为核心 36 + 契约内 68 = 104 项。 |
| v0.0.3 | 2026-07-29 | D8 议题收口：agents.md 项目级适配完成并生效——§八覆盖率门槛阶梯化（原值/适配值留痕）、§九工具链项目化、§二至§六术语本地化、§十一 Graph Engineering 按规模裁剪；无架构性变更。 |
| v0.1.0 | 2026-07-29 | **v0.1.0 发布**：MVP 最小闭环 + per-provider 强隔离架构。8 周分波（W1-W8）全部完成，详见 [docs/04-模块全景 §4](./docs/04-模块全景.md#4-v01-分波次交付计划6-8-周)。产出：81MB NSIS 安装包 + win-unpacked 目录 + [RELEASE_NOTES.md](./RELEASE_NOTES.md)。验收：typecheck 6 包过 / lint 0 error / 457 单测过 / 覆盖率 68.05% / E2E 4 过 / 冷启动 P95=1011ms / 10 标签内存 358.3MB。 |
| v0.1.0-dev.1 | 2026-07-29 | **W1-D1 脚手架 + IPC 契约层完成**。pnpm monorepo（apps/desktop + packages/ipc-contract/logger/chrome-types）；ESLint 9 flat + Prettier 3 + Husky + commitlint + CI；tsconfig strict + noUncheckedIndexedAccess；M17 IPC 契约层（zod schema 9 通道 + registerHandler 包装器 + typedInvoke 客户端 + IpcError 协议）；main/preload/renderer 入口（sandbox+contextIsolation）；Electron 32.3.3 二进制就绪。验证：typecheck 4 包全过 / lint 全过 / 37 测试全过（ipc-contract 34 + desktop 3）。 |
| v0.1.0-dev.2 | 2026-07-29 | **W1-D2 M1 Window Lifecycle + M18 Sandbox 完成**。WindowManager 类（多窗口管理 + 单调递增 windowId + 事件分发 + 自动清理）；BrowserWindowFactory 依赖注入（核心逻辑可单测）；createBrowserWindow 工厂（M18 sandbox+contextIsolation+preload 安全边界）；window.create/window.close IPC handler 注册；renderer App.tsx 增加窗口创建验证 UI；单实例锁 + second-instance 聚焦。验证：typecheck 4 包过 / lint 过 / 50 测试过（ipc-contract 34 + desktop 16：WindowManager 11 + App 5）。 |
| v0.1.0-dev.3 | 2026-07-29 | **W1-D3 M2 Tab Manager 完成**。TabManager 类（Tab 集合 + 按窗口分片 + 每窗口激活态 + 事件分发 created/updated/removed/activated/crashed）；webContents 生命周期事件绑定（did-start-loading/did-finish-load/did-navigate/page-title-updated/page-favicon-updated/render-process-gone）；TabSnapshot 序列化（剔除 webContents/view）；BrowserViewFactory 依赖注入；createBrowserView 工厂（M18 sandbox+contextIsolation）；tab.create/close/list/setActive/reload/goBack/goForward 7 通道 IPC handler 注册；主进程启动自动创建初始 tab。验证：typecheck 4 包过 / lint 过 / 69 测试过（ipc-contract 34 + desktop 35：TabManager 19 + WindowManager 11 + App 5）。 |
| v0.1.0-dev.4 | 2026-07-29 | **W1-D4 M3 Navigation Stack 完成**。TabManager.loadUrl/stopLoading 方法（webContents.loadURL/stop 包装 + 状态同步）；tab.loadUrl/tab.stop 2 新 IPC 通道（共 11 通道）；IPC schema 新增 tabLoadUrl/tabStop schema。验证：typecheck 4 包过 / lint 过 / 74 测试过（ipc-contract 34 + desktop 40：TabManager 24 + WindowManager 11 + App 5）。 |
| v0.1.0-dev.5 | 2026-07-29 | **W1-D5 W1 收口 + 覆盖率达标**。新增 tab/window IPC handler 注册单测（tab 10 + window 2）；coverage exclude 配置（types/index/entry point）；@vitest/coverage-v8 依赖；eslint 测试文件放宽 unbound-method + coverage 忽略。验证：typecheck 4 包过 / lint 过 / 86 测试过（ipc-contract 34 + desktop 52）/ 覆盖率 81.81%（≥60% 达标）。W1 验收信号：打开窗口 ✓ / 多标签 ✓ / 导航 URL ✓ / 关闭 ✓ / IPC 类型化 ✓ / sandbox+contextIsolation ✓。 |
| v0.1.0-dev.6 | 2026-07-29 | **W2-D1 M19 主题系统完成**。设计令牌系统（CSS 变量 12 色 + 间距 + 字体 + 阴影 + 圆角 + 动画）；Tailwind CSS 3 + Radix UI + Lucide Icons 集成；ThemeProvider（亮色/暗色切换 + data-theme 属性 + localStorage 持久化 + 系统偏好检测）；基础组件 Button（4 变体 + 3 尺寸 + loading + asChild）+ Input（前缀/后缀图标 + 错误状态）；cn 工具函数（clsx + tailwind-merge）。验证：typecheck 过 / lint 过 / 59 测试过（新增 ThemeProvider 6 + App 主题切换 1）。 |
| v0.1.0-dev.7 | 2026-07-29 | **W2-D2 M6 History + M5 Bookmarks 完成**。HistoryManager 类（内存存储 + URL 去重 + visitCount + 模糊搜索 + 按时间列表 + 删除/清空 + 事件分发）；BookmarkManager 类（内存存储 + 文件夹/书签 CRUD + 递归级联删除 + 模糊搜索 + 位置排序 + 事件分发）；history.record/search/list/delete/clear + bookmark.create/list/search/delete 9 通道 IPC handler；IPC schema 新增 history/bookmark schema（共 20 通道）。验证：typecheck 4 包过 / lint 过 / 99 测试过（新增 History 18 + Bookmark 20 + ipc-contract 1）。 |
| v0.1.0-dev.8 | 2026-07-29 | **W2-D3 M4 Omnibox 完成**。输入识别纯函数 parseInput（OM1 决策：URL/搜索词/内部资源/空 6 级判断链）；URL 安全校验纯函数 validateUrlBeforeNavigation（OM5 决策：封禁 javascript:/data:/vbscript: 协议）；补全建议类型 Suggestion（type/title/url/matchType/score）；Omnibox React 组件（150ms debounce 补全查询 + 回车导航 + Escape 恢复 + 焦点全选 + 安全状态图标 + 加载进度条 + 补全建议面板）。验证：typecheck 4 包过 / lint 过 / 122 测试过（新增 parseInput 13 + validateUrl 10）。 |
| v0.1.0-dev.9 | 2026-07-29 | **W2-D4 M7 Settings + M23 Download 基础完成**。SettingsManager 类（内存存储 + 6 项默认设置预填充 + get/set/getAll/has/delete + changed 事件分发）；DownloadManager 类（内存存储 + create/update/cancel/pause/resume/clear + 5 种状态机 progressing/completed/cancelled/interrupted/paused + 事件分发）；settings.get/set/getAll + download.list/cancel/pause/resume/clear 8 通道 IPC handler；IPC schema 新增 settings/download schema（共 28 通道）。验证：typecheck 4 包过 / lint 过 / 165 测试过（新增 Settings 21 + Download 22）。 |
| v0.1.0-dev.10 | 2026-07-29 | **W2-D5 M10-lite Extension Loader + W2 收口完成**。types.ts（ManifestV3 + ExtensionPermission 11 项 + ContentScript + BackgroundServiceWorker + LoadedExtension）；manifest-validator.ts 纯函数 parseManifest（JSON 解析 + manifest_version=3 强校验 + name/version 非空 + permissions 白名单 + content_scripts matches 非空 + 数组/非对象拒绝）；ExtensionLoader 类（从解压目录加载 + sha256 路径生成 32 字符确定性 ID + 重新加载替换 + get/list/enable/disable/remove/getCount）。验证：typecheck 4 包过 / lint 过 / 196 测试过（新增 manifest-validator 16 + extension-loader 15）/ 覆盖率 74.4%（≥60% 达标，M10 extensions 96.12%）。**W2 验收信号**：主题切换 ✓ / 历史记录 ✓ / 书签管理 ✓ / 地址栏输入识别 ✓ / 设置管理 ✓ / 下载状态机 ✓ / 扩展加载 ✓。 |
| v0.1.0-dev.11 | 2026-07-29 | **W3-D1 M8 Storage Layer 完成**。types.ts（IDatabase/IStatement/ISafeStorage/SecretStore/NamespaceStorage/DatabaseFacade/Migration 抽象接口）；sqlite-factory.ts（better-sqlite3 薄封装 + WAL 模式 + foreign_keys ON）；storage-layer.ts（主库 urchin.db + AI 库 ai.db + per-provider/per-extension 独立 db + LRU 连接池 max 10 + 按需 open/close）；secret-store.ts（safeStorage 加密 + 文件系统 secrets/<providerId>/<keyName>.enc + ST6 白名单路径穿越防护）；migrations.ts（schema_migrations 表版本追踪 + 主库 3 版 + AI 库 2 版迁移）。验证：typecheck 5 包过 / lint 过 / 226 测试过（新增 storage-layer 16 + secret-store 12 + migrations 2）。 |
| v0.1.0-dev.12 | 2026-07-29 | **W3-D2 M12 Provider Contract 包完成**。provider.ts（UrchinAIProvider 接口：manifest/initialize/dispose/complete/stream + 可选 embed/tools）；manifest.ts（ProviderManifest + ProviderCapability 7 项 + hasCapability + validateManifest zod 校验）；context.ts（ProviderContext + SecretStore + ProviderStorage 接口）；errors.ts（ProviderError 9 错误码 + isRetryable + RETRYABLE_ERROR_CODES + from 装箱）；messages.ts（CompletionRequest/Response/Chunk + TokenUsage + Embedding + ToolCall 类型）；version.ts（SUPPORTED_API_VERSIONS 硬匹配 IP2 决策）。验证：typecheck 5 包过 / lint 过 / 244 测试过（新增 ai-provider-contract 18）。 |
| v0.1.0-dev.13 | 2026-07-29 | **W3-D3 M11 Orchestrator 核心完成**。types.ts（ProviderHost + ITokenBucket + IMessagePort + IUtilityProcess + UtilityProcessFactory 抽象 + ProviderToOrch/OrchToProvider 消息类型）；orchestrator.ts（ProviderHost 生命周期：按需 spawn + 心跳监控 30s 超时 + 空闲回收 5min + crash 自动重建 + dispose 全清理 + 事件回调）；token-bucket.ts（OR4 令牌桶：capacityPerMin 限速 + 时间 refill + 阻塞 acquireRequestToken）；provider-registry.ts（扫描 providers/ 目录 + manifest.json 加载 + apiVersion 兼容校验 + entryPath 存在性检查）；electron-factory.ts（utilityProcess.fork + MessageChannelMain port 转移封装）。验证：typecheck 5 包过 / lint 过 / 274 测试过（新增 orchestrator 25 + token-bucket 10 + provider-registry 5）。 |
| v0.1.0-dev.14 | 2026-07-29 | **W3-D4 M11 Orchestrator 流式/重试 + M17 token 直通完成**。retry.ts（OR5 决策：callWithRetry 高阶函数 + NETWORK_ERROR/RATE_LIMITED 指数退避 1s/2s/4s 最多 3 次 + 不可重试错误立即抛出 + 非 ProviderError 装箱 UNKNOWN）；stream.ts（契约 I §6 流式链路：startStream 桥接 Renderer↔Provider Port + 透传 stream.chunk/stream.end/error + M17 token 直通 usage 字段 + Renderer abort 转发 + handle.abort() 幂等 + 流结束自动 cleanup）；types.ts 更新（stream.end 消息新增 usage?: TokenUsage）。验证：typecheck 5 包过 / lint 过 / 290 测试过（新增 retry 15 + stream 16）。 |
| v0.1.0-dev.15 | 2026-07-29 | **W3-D5 集成验证 + W3 收口完成**。修复 typecheck 错误（queueMicrotask 回调签名 + MockTokenBucket 类型 cast）；修复 lint 错误（unused eslint-disable + type→interface + unused vars）；修复 4 个 stream 测试时序问题（MockMessagePort 异步 queueMicrotask 投递需 await microtask 后再断言）。集成验证：M11→M12 类型导入正确（ProviderManifest/TokenUsage/ProviderError/CompletionRequest）+ M12→M8 SecretStore 结构兼容 + M11→M8 运行时解耦。验证：typecheck 5 包过 / lint 0 error / 290 测试全过 / 覆盖率 77.13%（Stmts）/ 89.08%（Branch）/ 88.06%（Funcs），Orchestrator 核心 retry/stream/token-bucket 均 100%。**W3 验收信号**：SQLite 持久化 ✓ / Provider 契约 ✓ / Orchestrator 生命周期 ✓ / 令牌桶限流 ✓ / 流式调用 ✓ / token 直通 ✓ / 重试策略 ✓。 |
| v0.1.0-dev.16 | 2026-07-29 | **W3.5 浏览器壳集成 + 打包验证**。修复 BrowserView 尺寸计算（getContentBounds）+ did-fail-load 状态重置；vite.config.ts mode 修正 + CJS/ESM 冲突处理 + electron 移至 devDependencies + App.test.tsx 重写 + electron-builder.yml 配置。打包产出 81MB NSIS 安装包（Urchin Browser-0.1.0-x64.exe）+ win-unpacked 目录。验收：导航问题修复 ✓ / 打包成功 ✓。 |
| v0.1.0-dev.17 | 2026-07-29 | **W4 AI 面板 UI + 页面上下文 demo 完成**。M13 AI Side Panel（对话状态管理 + 流式渲染 + 页面上下文注入）；M14 Page Context Extractor demo（Readability 正文抽取 + prompt builder）；IPC 接线（ai.chat.start/abort + provider.list + page.extract）；provider-status store + provider-warning-dialog UI。修复：mock 泛型签名 + regex 问题 + XML 转义 + ai-ipc-handlers 7 单测（覆盖率 0%→96%）。验证：typecheck 5 包过 / lint 0 error / 340 测试过 / 覆盖率 70.67%（Stmts）/ 87.8%（Branch）/ 86.95%（Funcs）。**W4 验收信号**：M13 Side Panel UI ✓ / M14 上下文抽取 demo ✓ / IPC 接线 ✓ / 流式对话状态管理 ✓ / 页面上下文注入 ✓。 |
| v0.1.0-dev.18 | 2026-07-29 | **W5 第三方 Provider 加载 + UI warning 流程完成**。ProviderRegistry 本地路径/npm 包安装 + manifest 校验 + 版本协商；IP8 安全 warning 对话（需输入"我确认"）；Provider crash 事件推送管道（Orchestrator→主进程广播→渲染 store→UI banner）；OR7 自动恢复（crash 后自动重建 + UI warning 清除）；Provider SDK 端到端集成（spawn→init→ready→stream→chunks→end）。修复：ElectronSafeStorage 导出 + MessagePortLike 类型 + 29 lint 错误。验证：typecheck 6 包过 / lint 0 error / 381 测试过 / 覆盖率 68.05%（Stmts）/ 87.67%（Branch）/ 87.03%（Funcs）。**W5 验收信号**：第三方 Provider 加载 ✓ / IP8 安全 warning ✓ / crash 事件推送 ✓ / OR7 自动恢复 ✓ / Provider SDK E2E ✓。 |
| v0.1.0-dev.19 | 2026-07-29 | **W6 收口 + 测试 + CI 完成**。E2E 关键路径修复：appPath 路径修正（resolve(__dirname, '..')→resolve(__dirname, '..', '..')）+ 改用 Playwright 官方 _electron.launch API 替代手动 spawn+connectOverCDP；清理 12 个调试脚本/日志。CI 流水线完善：E2E 前置 build 步骤 + 移除无效 DISPLAY env。验证：typecheck 6 包过 / lint 0 error / 457 单测过（ipc 34 + ai-contract 42 + sdk 11 + desktop 370）/ 覆盖率 68.05%（Stmts）/ 87.67%（Branch）/ 87.03%（Funcs）/ E2E 4 过（9.9s）。**W6 验收信号**：单测覆盖率达标 ✓ / E2E 关键路径跑通 ✓ / pre-commit hook 启用 ✓ / GitHub Actions 流水线绿 ✓。 |
| v0.1.0-dev.20 | 2026-07-29 | **W7 体验调优 + 文档完善完成**。性能验收：冷启动 P95=1011ms（≤3000ms ✓，10 次 warmup 后测量 min=889ms/p50=894ms/max=1011ms）；10 标签内存 358.3MB（≤500MB ✓，4 进程：Browser 119.6+GPU 99.5+Utility 51.6+Tab 87.6）。性能测试脚本：perf.ps1（冷启动 11 次 warmup+10 测量）+ perf.spec.ts（Playwright _electron.launch + app.getAppMetrics 内存测量）。README 完善：v0.1 功能概览表 + 快速开始 + 开发命令 + 性能指标 + 架构亮点。**W7 验收信号**：启动 ≤3s ✓ / 内存 ≤500MB ✓ / CHANGELOG 与 README 完善 ✓。 |
| v0.1.0-dev.21 | 2026-07-29 | **W8 发布准备完成**。重新构建 + 打包：pnpm build（main 197KB + preload 62KB + renderer 190KB+14KB CSS）+ pnpm package:win 产出 81MB NSIS 安装包（Urchin Browser-0.1.0-x64.exe）+ win-unpacked 目录 + blockmap。RELEASE_NOTES.md 创建：功能清单 + 性能指标 + 质量指标 + 安装说明 + 已知限制 + v0.2 规划。无代码签名证书（SmartScreen 可能警告）。**W8 验收信号**：electron-builder 产出可安装 Windows 包 ✓ / release notes ✓。 |
| v0.1.0-dev.22 | 2026-08-02 | **覆盖率冲刺至 70.67% + 仓库清理 + 重新打包**。多轮补测：urchin 协议/设置页/窗口工厂/书签历史设置 IPC/Input 与 ProviderWarningDialog/ElectronFactory-SQLite-安全存储-下载 IPC/SettingsManager 持久化异常分支/ThemeProvider getInitialTheme 分支，整体单元覆盖率 62.99%→**70.67%**（581 测试全绿，v0.2 ≥65% 线已破）。冗余清理：删空目录（renderer/stores、side-panel）+ tsbuildinfo/launch 日志/24 个调试脚本，同步修正 docs/03-技术栈 §3 目录树、docs/07 与契约包路径引用。重新打包：`electron-builder --win --x64` 产出 **83MB NSIS 安装包（Urchin Browser-0.1.0-x64.exe）** + win-unpacked + blockmap（release-v25；release-v18/19/23/24 因 asar 被外部进程锁定待无占用后清理）。 |
| v0.1.0-dev.23 | 2026-08-14 | **隔离止血 + 仓库清理 + 地址栏补全 + 覆盖率冲刺至 89.21%**。安全治理（审计 P0-1/P0-2/P0-3）：ai.agent.start 强制 enableTools=false（v0.1 对话路径在主进程运行，coding 工具挂载可执行任意命令；恢复条件=对话迁回 utility 子进程）；ai.apiKey/summary.apiKey 改走 SecretStore（safeStorage）加密落盘，不再明文写 SQLite（旧明文 set 时自动清除，settings-manager 增可选 secretStore 参数 + ensureSecretsLoaded）；删除 provider.install/provider.remove IPC 链路与 provider-warning-dialog 组件（生产零引用、未交付死链路，registry 方法保留）；删除主进程启动诊断 console.error 两处。清理：遗留目录 ×4（temp-test/test-output/.test-user-data/.electron-cache）+ 调试脚本 ×4（cdp-client/close-window×2/delayed-close）+ 死依赖 ×6（xstate/zustand/react-dialog/react-tooltip/ws/user-event）+ eslint ignores ×4 + orchestrator/index.ts 死中转。功能：M4 地址栏补全接线——新增 build-suggestions.ts 纯函数（历史+书签合并/评分/去重/排序，url 精确/子串/title 命中+前缀+书签+热度评分），App.tsx handleSuggestionQuery 由空实现 stub 改为并联 history.search+bookmark.search 并加查询竞态防护。测试：覆盖率 70.67%→**89.21%**（635 测试全绿，v1.0 ≥80% 门槛已破），新增 SettingsPage 组件 8 / omnibox 15 / pi-settings-dialog 9 / build-suggestions 13 用例 + tab-manager 追加 12（webContents 生命周期 + window.open 双行为 + disposeAll）+ App 追加 9（摘要树/AI 与设置入口/书签 toggle/summary.run/tab:event 四分支）。验证：typecheck 9 包 / lint 0 error / prettier 过 / 635 测试全绿 / 覆盖率 89.21%。注：提交被 Mimosa L3 门禁误报阻塞（7 项高危均为历史存量文件误报），用户已禁用该插件，重启会话后 fix/chore/feat/test 四笔提交落库。 |
| v0.1.0-dev.24 | 2026-08-17 | **确立「生产级标准 + 长期迭代承诺」正式声明（docs）**。按用户指令将「本项目以生产级标准开发与验收，相关规范与约定满足长期迭代需求」确立为正式声明并落盘：agents.md 新增「项目声明」章节（声明一 开发标准——生产级是自 v0.1 起的全过程标准而非 v1.0 目标；声明二 验收标准——一切交付以确定性信号验收、不接受 AI 自评；声明三 长期迭代承诺——可追溯/可演进/可维护/可复核，量化门槛与流程调整仍须走 §〇.3 红线与 §〇.4 权限机制），性质为对既有条款的明示与固化（填充细化，无架构性变更），适配说明区追加调整项第 6 条 + 速查表追加「项目声明」行；docs/00-设计总览 §2 追加设计原则第 6 条「生产级标准」+ §5 补声明说明；docs/04-模块全景 与 docs/05-路线图 的 v1.0「生产级就绪」改为「生产级就绪里程碑（标准自 v0.1 全程适用，声明见 agents.md）」。验证：prettier 格式校验过；纯文档改动，不影响 typecheck/lint/测试。 |
| v0.1.0-dev.25 | 2026-08-17 | **右侧边栏交互/视觉打磨 + 恢复 GPU 硬件加速（用户授权）**。UI（renderer）：① 右侧边栏双击展开/折叠仅响应空白处——按点击目标排除标签卡片（data-tab-card）/按钮/宽度拖拽手柄（role=separator），空白区（底色/标题区/滚动留白）才触发；② 标签关闭按钮 X 改红色（text-error + hover 红底）；③ 折叠态标签图标方形小圆角（rounded-lg→rounded-md）；④ 折叠态图标加大：按钮 40px（栏宽 44px 左右各留 2px）+ 图标 36px 铺满按钮内部 + 图标间距 2px；⑤ 无法识别的网站图标改半透明浏览器图标占位（主页 SiteFavicon + 侧栏 TabSiteIcon，opacity-40 淡化避免颜色点亮，替换原灰点占位/全亮图标）；⑥ 左侧边栏背景改标题栏同色令牌——新增 `--color-titlebar`（亮色 #ffffff / 暗色 #202020 中性深灰，贴近 Win11 原生标题栏明暗），Tailwind 注册 titlebar 色，左侧栏 bg-surface→bg-titlebar（暗色下消除蓝黑 #0f172a 与系统标题栏的色差），同步设置页调试配色变量列表。主进程（fix）：**恢复 GPU 硬件加速**——删除 `app.disableHardwareAcceleration()`（2026-08-17 曾因三卡混合环境 TDR 桌面卡死而禁用，代价为全网页 SwiftShader CPU 软件渲染致打开重页面卡顿；经用户决策恢复，代码注释留痕决策与复发预案）。验证：typecheck 全包过 / lint 本批文件 0 error / 实测 GPU 生效（gpu-process 无 swiftshader 参数）且长时间使用桌面无卡死复发。决策留痕：docs/decisions/决策索引 GR1。 |
| v0.1.0-dev.26 | 2026-08-17 | **可配置数据目录 + pi 数据隔离 + 完整下载 + 地址栏截图按钮**。数据架构（DD1 决策）：① 新增 data-location 机制——数据目录指针 `userData/data-location.json`，默认 `<软件根目录>/data`（开发模式 = apps/desktop/data；无指针时若旧 `userData/data` 已有数据则整体自动迁入新默认并清理旧目录），`data.directory` 设置可改（重启迁移生效）；② **pi(AI) 数据隔离**——`urchin://` pi 数据（ai.db / secrets / providers 命名空间）固定存 `userData/pi`，设置管理器 pi 键（ai.* / summary.*）路由到 pi 库 + pi 密钥存储，旧主库残留 pi 键自动迁移；升级迁移自动整理旧布局（data/ai.db→pi/ai.db、userData/summaries→data/summaries）；摘要文档默认目录随数据目录。功能：③ 完整下载流程（DL1 决策）——will-download 挂钩：已设 downloadsPath 直达；未设弹保存对话框（默认 `<数据目录>/downloads/`）→ 确认后询问「设为默认下载路径」（勾选写 downloadsPath），进度/状态同步 DownloadManager；④ 地址栏截图按钮（Camera 图标，收藏夹按钮后）——screenshot.capture IPC 弹整屏框选覆盖窗口，确认后裁剪保存 `<数据目录>/screenshots/` 并打开所在文件夹（详见 dev.27 框选交互）。设置页新增「数据存储位置」目录字段。验证：typecheck 全包过 / lint 0 error / 700 单测全绿（新增 data-location 9 + settings-manager-pi 6 + capture-overlay 8）。决策留痕：docs/decisions/决策索引 DD1/DL1。 |
| v0.1.0-dev.27 | 2026-08-17 | **整屏框选截图 + 默认数据目录改软件根 + 移除摘要目录设置**。① 截图交互升级——点击地址栏截图按钮弹**全屏框选覆盖窗口**（透明 + alwaysOnTop 独立 BrowserWindow，背景 = desktopCapturer 主屏物理分辨率截图；拖拽画半透明选区，选区右下角悬浮「取消」「确认」按钮，Esc 取消；确认后主进程 NativeImage.crop 裁剪 → 存 `<数据目录>/screenshots/<时间戳>.png` → 打开所在文件夹）；新增 IPC：screenshot.capture（弹窗）/ getImageData（背景图）/ confirm（裁剪保存）/ cancel（取消）；urchin://capture-overlay 协议路由。② 默认数据目录从 `userData/data` 改为**软件根目录/data**（用户决策：默认+自动创建替代首次弹窗引导；无指针且旧位置有数据时整体自动迁移；生产打包后软件根可能只读，v1 打包时评估回退）。③ 移除「摘要文档保存位置」设置项（摘要文档已移入数据目录，恒定 `<数据目录>/summaries`）——SettingsPage/settings-manager/index.ts 及对应测试同步清理。验证：typecheck 全包过 / lint 0 error / 700 单测全绿。 |
| v0.1.0-dev.28 | 2026-08-17 | **广告浮窗屏蔽（DB1 决策）+ 右侧边栏展开态标签图标铺满**。① 广告浮窗屏蔽——设置页隐私与安全新增「屏蔽广告浮窗」开关（blockAds，默认开启）；主进程 tab-manager 在 did-finish-load 后经 webContents.insertCSS 注入隐藏 CSS（仅浮窗类：position:fixed 且 id/class 含广告特征的选择器 + .ad/.ads/.advert/.adsense/.ad-container/.ad-modal/.popup-ad 等常见浮窗容器；只隐藏不拦截请求，避免误伤）；设置变更实时联动（开启时对已加载 tab 补注入）；WebContentsLike 接口补 insertCSS 声明；规则集抽为 buildAdBlockCss 纯函数（6 单测）。② 右侧边栏展开态标签图标铺满高度——TabFavicon 由默认 12px 改为 h-9 w-9（36px，与折叠态一致，img object-contain 不变形，items-center 垂直居中）。验证：typecheck / lint 0 error / 709 单测全绿。决策留痕：docs/decisions/决策索引 DB1。 |
| v0.1.0-dev.29 | 2026-08-18 | **本地文件网页化打开（音视频/PDF/图片原生渲染 + 文本类文档查看器）**。① 打开入口 ×4——左侧边栏底部「打开文件」按钮（AI 助手上方，FolderOpen）+ Ctrl+O（主进程已移除全局菜单，渲染层 keydown 监听）+ 文件拖放窗口 + 地址栏本地路径（`C:\x.mp4` / `\\server\share` 自动补 file:// 前缀并百分号编码）。② 双路由——音视频/PDF/图片/HTML → `file://` 直开标签页（Chromium 原生播放器/PDF 查看器，零依赖）；md/txt/json/log/代码 → 新内部页 `urchin://file-viewer?path=`（主窗口 React FileViewer 渲染：md 用 marked@18 转 HTML——禁用 raw HTML + 链接协议白名单防 XSS，json 自动美化，txt 纯文本 `<pre>`；5MB 读取上限超限提示「文件过大」）。③ 基础设施——IPC 新增 file.stat/read/open 三通道 + FILE_TOO_LARGE 错误码；主进程 files/ 模块（file-kind 扩展名→类型分类表 90+ 扩展名，单一真源）；file-viewer 协议 host + view-integration URCHIN_RENDERER_HOSTS 注册；依赖 marked@18.0.5（与 vendor/pi 同版本，pnpm 复用零下载）。修复：中文/空格路径未编码致 loadURL ERR_FAILED（encodeURI 兜底，实测拖放中文路径视频触发）。验证：typecheck / lint 0 error / 755 单测全绿（新增 65 用例）/ 生产构建三路成功 / 运行实测拖放入口链路走通。 |
| v0.1.0-dev.30 | 2026-08-18 | **Windows 文件关联：可设为音视频/文档/图片的默认打开软件**。① 设置页新增「默认应用」选项卡——三组卡片（音视频/文档/图片）展示扩展名清单与注册状态（n/total 已关联徽标）+「设为默认打开方式」按钮 + 说明文案（注册后到系统「打开方式→选择其他应用」勾选「始终使用」即由 Windows 写 UserChoice 完成永久默认；不直接写 UserChoice hash——系统带签名校验，官方路径更可靠）。② 关联注册——主进程 file-association/ 模块：扩展名单一真源复用 file-kind 分类表（getExtensionsForKind 反查），每个扩展名写 HKCU 注册表 5 项（.ext 默认值=ProgID + OpenWithProgids 登记 + ProgID 描述 + shell\open\command="<exe>" "%1" + DefaultIcon），经系统自带 reg.exe 执行（execFile 依赖注入可单测），用户级 HKCU 无需管理员权限。③ 接收打开请求——启动 process.argv 解析 + second-instance commandLine 解析（兼容裸路径/file:// URL），统一 openLocalFile 复用本地文件查看路由（原生 kind 直开 file://，文本走查看器）。④ IPC 新增 file-association.register/getStatus 两通道。验证：typecheck / lint 0 error / 779 单测全绿（新增 24 用例）。 |
| v0.1.0-dev.31 | 2026-08-18 | **文件夹浏览 + 同目录同类型连续查看**。① 打开文件夹——左侧栏新增「打开文件夹」按钮（Folder 图标，打开文件按钮上方）→ file.openFolder 选目录 → 目录浏览页（?dir=）：文件夹网格（子目录在前 + 文件按名排序），点子目录进入、点文件预览，返回上一级按钮（盘符根禁用），空目录提示。② 媒体/PDF 收进 FileViewer 内嵌渲染（架构调整：audio/video/pdf/image 不再 file:// 直开独立标签页，改为查看器内 <img>/<video>/<audio>/<iframe>（Chromium 内置 PDF 查看器保留缩放/翻页/打印）），html 仍 file:// 直开（内嵌 iframe 相对资源解析问题，不进序列）。③ 同类型连续查看——读同目录经 file.dir 过滤同 kind 组（图片组/视频组/音频组/PDF 组/文档组 markdown+json+text），工具栏「上一个/下一个」按钮 + n/total 计数 + 键盘 ←/→ 同标签内导航（tab.loadUrl，不堆标签）。④ file-resource 协议 host——urchin://file-resource/<编码路径> 流式返回本地文件（fs.createReadStream），支持 Range 请求（视频拖动 seek，206 + Content-Range 分段），Referer 必须 urchin:// 否则 403（urchin: 配了 corsEnabled，不校验 = 任意网页可跨源盗读本地文件）；响应计划抽为 planLocalFileResponse 纯函数（403/200/206/416 分支全单测）。⑤ IPC 新增 file.dir（readdir+stat 排序，注入 mock 可单测）/ file.openFolder 两通道。验证：typecheck / lint 0 error / 805 单测全绿（新增 26 用例）。 |
| v0.1.0-dev.32 | 2026-08-18 | **网页右键菜单：复制 / 粘贴 / 保存网页**。新增 main/context-menu 模块——网页 tab 的 BrowserView 与**主窗口 webContents（新标签页/设置页等 React 内部页）**都挂接 context-menu 事件，弹出**原生菜单**（与 Chrome 一致，主进程弹出、渲染进程零改动；不使用 preload 注入——固化教训 BrowserView 注入 preload 会阻塞网页加载）：① 复制/粘贴——按 Electron editFlags.canCopy/canPaste 动态置灰（无可复制内容/非输入框焦点时不响应），动作经 webContents.copy()/paste()（Chromium 编辑命令，选中文本直接进系统剪贴板）；② 保存网页为…——**单独立项（用户决策）**：默认目录恒为 `<数据目录>/saved-pages/`（自动创建，不复用下载目录 downloads/、不读 downloadsPath 设置、**不接入 DownloadManager**，仅依赖数据目录），默认文件名取页面标题、非法字符清洗、手删扩展名自动补 .html → webContents.savePage('HTMLComplete') 整页保存（HTML+资源目录，双击可离线打开）；urchin:// 内部页（React 渲染）置灰「保存网页」；③ 真实鼠标右键坐标补偿——BrowserView 的 params 坐标相对页面、popup 坐标相对窗口，按 BrowserView 边界偏移量补偿（否则菜单出现在鼠标左侧 44px）；④ TabManager 新增 getTabByWebContents（context-menu 事件携带 webContents 实例，经此反查 tab 归属取 url/title）。修复：新标签页右键无菜单——此前只挂 BrowserView（外部网页），主窗口内部页未挂监听；E2E 探针实测主窗口/网页两路右键均触发 Menu.popup 且无控制台报错。退出清理移除 created 监听。验证：typecheck 9 包 / lint 0 error / 837 单测全绿（context-menu 24 + tab-manager 3）/ prettier 过 / E2E 实测两路触发。 |
| v0.1.0-dev.33 | 2026-08-18 | **Ctrl+滚轮缩放网页（v2，修复用户反馈「未实现」）**。**v1 失效根因**：第一版用 `before-input-event` 拦截滚轮，但 Electron 32 该事件只在键盘事件（`PreHandleKeyboardEvent`）发射，滚轮永不触发（Electron 源码证实），功能完全不生效。**v2 方案**：向每个 webContents（网页 tab 的 BrowserView + 主窗口内部页）注入 wheel 监听脚本（`executeJavaScript`，did-finish-load 后执行 + 整页导航重注入 + `__ubZoomInstalled` 幂等标志，不用 preload——固化教训），识别 Ctrl+滚轮（`e.ctrlKey`）后 `preventDefault()` 阻止页面滚动并 `fetch('urchin://zoom[-main]?d=in|out', {mode:'no-cors'})` 通知主进程；urchin: 特权协议（bypassCSP + supportFetchAPI + corsEnabled）保证任意网页（含严格 CSP）均可发请求；主进程按方向调整 zoomFactor：向上 ×1.1 放大 / 向下 ÷1.1 缩小，clamp 25% ~ 500%（与 Chrome 一致）。路由：`urchin://zoom` → 网页 tab 缩放（active tab）、`urchin://zoom-main` → 主窗口内部页缩放。缩放状态由 Electron 按 webContents 持有（每标签独立、切 tab 保留），模块不维护额外状态；退出清理移除 created/did-finish-load 监听。验证：typecheck 9 包 / lint 0 error / prettier 过 / **859 单测全绿**（zoom 15 + protocol 缩放路由 4）/ **E2E 实测缩放生效**——主窗口 Ctrl+滚轮 zoomFactor 1→1.1→1.21，网页 BrowserView 1→0.909→1.0，无 Ctrl 滚轮不缩放。 |
| v0.1.0-dev.34 | 2026-08-19 | **标签/书签拖出浏览器窗口 → 新窗口打开（v3：移动语义 + 跟随拖拽位置）**。**v1 问题**：HTML5 系统拖放（text/uri-list）→ Windows 对拖到桌面的 URL 默认生成快捷方式。**v2 方案**：自定义指针拖拽——右侧边栏标签（展开卡片 + 折叠图标）与收藏夹面板书签 pointerdown 记录 URL + `setPointerCapture`，pointermove 检测指针移出窗口边界（clientX/Y <0 或 >视口）→ `window.createWithUrl` 新窗口打开；仅 http/https 可拖，内部页不响应；标签卡片加入窗口拖拽排除列表。**v3（用户反馈）**：① 拖出后**关闭原窗口标签**（移动语义，原窗口不再保留该标签）——createWithUrl 成功后 `tab.close` 原标签；② 新窗口**定位到拖出瞬间的屏幕坐标**（跟随拖拽位置，不重叠原窗口）——渲染层传 `e.screenX/screenY`，主进程经 `screen.getPrimaryDisplay().workArea` clamp（至少 120px 窗口主体可见）后 `setPosition`（指针移出 OS 窗口后渲染进程收不到事件，故用出界瞬间坐标，出界即开窗、松手即落位）；③ IPC `window.createWithUrl` schema 增可选 x/y 坐标（zod 校验）。验证：typecheck 9 包 / lint 0 error / prettier 过 / **863 单测全绿 + ipc-contract 34**（新增 App 出窗 3 + 面板模板 1 + schema 1）。 |
| v0.1.0-dev.35 | 2026-08-19 | **修复：新拖拽窗口内组件全部失效（多窗口支持）**。**根因**：渲染层约 11 处硬编码 `windowId: 1`（App.tsx 7 / host-impl 3 / FileViewer 1）——新窗口是 windowId=2，其 React UI 的 tab.list/tab.create 全在操作原窗口的标签，组件自然失灵。**修复**：① 主进程新增 `window.getCurrent` IPC + `WindowManager.getWindowByWebContents`（按 event.sender 引用相等反查所属窗口）；② 渲染层新增 `lib/current-window.ts` 的 `getCurrentWindowId()`（首次走 IPC、模块级缓存——每窗口渲染进程独立 JS 上下文天然隔离；异常回退 1）；③ App.tsx 全部 7 处、host-impl 3 处、FileViewer 1 处硬编码 `windowId: 1` 改为 `await getCurrentWindowId()`；④ `ui.window.dragBy` 同步改为按 sender 反查窗口移动（新窗口拖窗不再误移原窗口）；⑤ 收藏夹面板（依附主窗口）保持 1 不动。验证：typecheck 9 包 / lint 0 error / prettier 过 / **864 单测全绿 + ipc-contract 34**（新增 getWindowByWebContents 1 + schema 1）。 |
